export const insialState = {
      authenticated :{
        user :{
        },
        accessToken : localStorage.getItem('access_token') ,
      },
      projects :[],
      handymans :[],
      nearerHandymans :[],
}